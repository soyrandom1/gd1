extends Node2D

#const first = preload("res://miniscenes/loadtest.tscn")
#var t = true
func _ready():
	set_fixed_process(true);

func _fixed_process(delta):
	if(t):
		#var firstV = first.instance()
		#firstV.set_pos(Vector2(0,0))
		#get_tree().get_root().add_child(firstV)
		#t = false
		pass